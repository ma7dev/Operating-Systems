#include "consumer.h"

/*
Takes the next element off the buffer
*/
void consume()
{
    //Make random
    //sleep(2);
}

/*
Returns 0 for empty, 1 for non-empty
*/
int isBufferEmpty()
{
    return 0;
}
